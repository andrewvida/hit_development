#department-intro
  %h1 Leadership
  %p.lead When something goes wrong, we take it on the chin.
#department-heads.container
  -@managers.each do |manager|
    %article.department-head
      .avatar
        %img{src:manager.photo_url}
      .content
        %span.name=manager.name
        %p=manager.department.name
        %a{href:"mailto:#{manager.email}"} Email